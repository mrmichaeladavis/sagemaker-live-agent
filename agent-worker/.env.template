# -----------------------------------------------------------------------------
# API Keys for LiveKit Plugins
# -----------------------------------------------------------------------------
# The plugins automatically read these keys from the environment.
# Replace the placeholder values with your actual API keys.

# -----------------------------------------------------------------------------
# Application Configuration Overrides
# -----------------------------------------------------------------------------
# Use the format PARENT__CHILD=value to override nested settings.

# --- General Agent Settings ---
# AGENT_INSTRUCTIONS="You are a pirate captain. Respond with a hearty 'Ahoy!' and keep it brief."


# --- Primary LLM Configuration ---
# Example: Use a local model or proxy (like LiteLLM) by overriding the base_url.
# This is only compatible with providers that use an OpenAI-like client (e.g., 'openai').
#
PRIMARY_LLM__PROVIDER="openai"
PRIMARY_LLM__MODEL="google-gemini-2.5-pro"
PRIMARY_LLM__BASE_URL="https://warren.app-dev.chaosfractal.io/api/v1/llm/"
PRIMARY_LLM__API_KEY=""
PRIMARY_LLM__TEMPERATURE=0.1
PRIMARY_LLM__TOP_P=0.1
PRIMARY_LLM__COST_PER_INPUT_TOKEN=0.00000125 # $1.25 per 1M input tokens
PRIMARY_LLM__COST_PER_OUTPUT_TOKEN=0.00001 # $10 per 1M output tokens

# --- Fast LLM Configuration ---
# Example: Change the fast LLM to a different model if needed.
#
# FAST_LLM__MODEL="llama3-8b-8192"
FAST_LLM__PROVIDER="openai"
FAST_LLM__MODEL="google-gemini-2.5-flash-lite"
FAST_LLM__BASE_URL="https://warren.app-dev.chaosfractal.io/api/v1/llm/"
FAST_LLM__API_KEY=""
FAST_LLM__TEMPERATURE=0.1
FAST_LLM__TOP_P=0.1
FAST_LLM__COST_PER_INPUT_TOKEN=0.0000001 # $0.10 per 1M input tokens
FAST_LLM__COST_PER_OUTPUT_TOKEN=0.0000004 # $0.40 per 1M output tokens

# --- TTS (Text-to-Speech) Configuration ---
# Example: Change the TTS voice for the default OpenAI provider.
#
# TTS__PROVIDER="openai"
# TTS__MODEL="tts-1"
# TTS__VOICE="alloy"
# TTS__API_KEY=""

TTS__PROVIDER="openai"
TTS__MODEL="local-kokoro-tts"
TTS__VOICE="af_heart"
TTS__BASE_URL="https://warren.app-dev.chaosfractal.io/api/v1/llm/"
TTS__API_KEY=""

# TTS__PROVIDER="elevenlabs"
# TTS__MODEL="eleven_flash_v2_5"
# TTS__VOICE="Dorian" # Not Used for elevenlabs but needed for config validation
# TTS__VOICE_ID="bgWMNrX74qZEgJ5LcEBB"
# TTS__API_KEY=""

# --- STT (Speech-to-Text) Configuration ---
# Example: Change the STT provider from Deepgram to AWS Transcribe.
#
# STT__PROVIDER="aws"
# STT__LANGUAGE="en-US"
# STT__COST_PER_SECOND=0.0004166 # $0.025/min

STT__PROVIDER="deepgram"
STT__LANGUAGE="en-US"
STT__MODEL="nova-3"
STT__API_KEY="058816982efa2142064976ad7e50a6a7fefd1673"
STT__COST_PER_SECOND=0.0000717 # $0.0043/min

# STT__PROVIDER="openai"
# STT__MODEL="local-whisper-large-v3"
# STT__BASE_URL="https://warren.app-dev.chaosfractal.io/api/v1/llm/"
# STT__API_KEY=""


# --- VAD (Voice Activity Detection) Configuration ---
# Fine-tune the Silero VAD.
# - activation_threshold: (0 to 1) Lower values make it more sensitive to speech.
# - min_silence_duration: (seconds) Longer pauses required to trigger end-of-speech.
#
# VAD__ACTIVATION_THRESHOLD=0.4
# VAD__MIN_SILENCE_DURATION=0.3

LIVEKIT_API_SECRET=
LIVEKIT_API_KEY=
LIVEKIT_URL=ws://container02:7880
